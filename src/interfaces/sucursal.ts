export interface Sucursal {
    nom_Suc?: string;  // Nombre de la sucursal
    loc_Suc?: string;  // Localización de la sucursal
    des_Suc?: string;  // Descripción de la sucursal (texto largo)
    img_Suc?: string;  // Imagen de la sucursal (URL o nombre de archivo)
}
